#include "state.h"
#include "stateTree.h"

#define CAPACITY (16)

int heap[CAPACITY];

void setup() {
  //zeroOutHeap(heap, capacity);
}

void loop() {
  //Serial.println(heap[0]);  
  Serial.println(CAPACITY);
}
